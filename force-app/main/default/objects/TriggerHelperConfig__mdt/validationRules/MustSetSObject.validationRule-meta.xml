<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>MustSetSObject</fullName>
    <active>true</active>
    <description>One of the two SObject fields must be set.   Used to ensure that non-compliant standard objects producing a null SObjectType__c value will properly be supported.</description>
    <errorConditionFormula>AND(ISBLANK(SObjectType__c)
,ISBLANK(AlternateSObject__c))</errorConditionFormula>
    <errorMessage>You must set the SObject either in the main field or the alternate field</errorMessage>
</ValidationRule>
